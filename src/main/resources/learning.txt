character space is ' ' not ''
always use functional increment inside the loop
make sure to consider the last word in your logic
String.valueof(charArray) will print in the form of String whereas charArray.toString() will just print hashcode
always use else if

